# PostgreSQL (metadata store: schema iks for backup jobs and history)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=postgres
POSTGRES_USER=your_user
POSTGRES_PASSWORD=your_password

# Optional
# BACKUP_UI_PORT=3100
# BACKUP_BINARY=/path/to/Backup
# BACKUP_STORAGE_DIR=/path/to/backup/files
