# Backup – variables de entorno (copiar a backup.env o .env)
# Solo se usa este archivo o las variables en el sistema; no hay config.json.

# PostgreSQL donde se guardan los backups programados y el historial (metadata)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=postgres
POSTGRES_USER=backup
POSTGRES_PASSWORD=secret

# Opcionales para la Web UI (ui/backend)
# BACKUP_UI_PORT=3100
# BACKUP_BINARY=/ruta/absoluta/a/build/Backup
